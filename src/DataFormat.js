/**
 * Enumeration of the supported data formats.
 * @type {Object.<string,string>}
 */
const DataFormat = {
    JSON: 'json',
    PNG: 'png',
    SB2: 'sb2',
    SVG: 'svg',
    WAV: 'wav'
};

module.exports = DataFormat;
